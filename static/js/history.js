document.addEventListener('DOMContentLoaded',async()=>{const list=document.getElementById('historyList');const clear=document.getElementById('clearHistory');async function load(){const r=await fetch('/api/history');const d=await r.json();if(!d.items.length){list.innerHTML='<div class="empty-state"><span>◷</span><p>No operations yet.</p></div>';return}list.innerHTML=d.items.map(x=>`<div class="history-item"><div><b>${esc(x.operation)}</b><span>${esc(x.original)}</span></div><small>${esc(x.time)}</small></div>`).join('')}clear.onclick=async()=>{await fetch('/api/history/clear',{method:'POST'});load()};load();function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}});
